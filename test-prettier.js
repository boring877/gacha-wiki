// Test file to verify Prettier formatting
const _testFunction = () => {
  const _message = 'testing prettier formatting';
  return {
    success: true,
    message: _message,
    data: [1, 2, 3, 4, 5],
  };
};
