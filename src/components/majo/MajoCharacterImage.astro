---
// MAJO character art, served from the gachawiki-images R2 bucket (keeps the
// Pages deployment under the 20,000-file limit). Flat /majo/ namespace, webp.
const R2 = 'https://pub-dd9a9c01bc7a43d0bb977b255815a5c4.r2.dev/majo';

export interface Props {
  filename: string;
  alt?: string;
  class?: string;
  loading?: 'eager' | 'lazy';
}

const { filename, alt = 'Character', class: className = '', loading = 'lazy' } = Astro.props;
const src = `${R2}/${filename.replace(/\.png$/, '.webp')}`;
---

<img src={src} alt={alt} class={className} loading={loading} decoding="async" width="293" height="512" />
