---
// Make Drama character image component — images served from Cloudflare R2
const R2 = 'https://pub-dd9a9c01bc7a43d0bb977b255815a5c4.r2.dev/make-drama/characters';

export interface Props {
  filename: string;
  alt?: string;
  class?: string;
  loading?: 'eager' | 'lazy';
}

const { filename, alt = 'Character', class: className = '', loading = 'lazy' } = Astro.props;
const imageSrc = filename ? `${R2}/${filename}` : null;
---

{imageSrc ? (
  <img src={imageSrc} alt={alt} class={className} loading={loading} decoding="async" />
) : (
  <div class={`md-image-fallback ${className}`} style="background: var(--md-bg-secondary); border-radius: 8px; display: flex; align-items: center; justify-content: center; color: var(--md-text-muted); font-size: 0.75rem; min-height: 100px;">
    {alt}
  </div>
)}
