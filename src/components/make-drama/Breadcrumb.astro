---
import {
  generateBreadcrumbs,
  getParentBreadcrumb,
  generateBackNavigation,
} from '../../utils/breadcrumb-generator.js';
import type { BreadcrumbItem, BackNavigation } from '../../utils/breadcrumb-generator.js';

export interface Props {
  items?: Array<{
    name: string;
    href?: string;
  }>;
  autoGenerate?: boolean;
  gameKey?: string;
  currentPageName?: string;
  includeHome?: boolean;
  showBackButton?: boolean;
}

const {
  items,
  autoGenerate = false,
  gameKey,
  currentPageName,
  includeHome = true,
  showBackButton = true,
} = Astro.props;

let breadcrumbItems: BreadcrumbItem[] = items || [];
let backNavigation: BackNavigation | null = null;

if (autoGenerate) {
  breadcrumbItems = generateBreadcrumbs(Astro.url.pathname, {
    includeHome,
    currentPageName,
    gameKey,
  });

  if (showBackButton) {
    backNavigation = generateBackNavigation(Astro.url.pathname, { gameKey });
  }
} else if (showBackButton && items) {
  const parent = getParentBreadcrumb(items);
  if (parent) {
    backNavigation = {
      href: parent.href || '/',
      text: `Back to ${parent.name}`,
      parentName: parent.name,
    };
  }
}
---

<nav class="md-breadcrumb" aria-label="Breadcrumb">
  <div class="md-breadcrumb-container">
    {
      backNavigation && (
        <button
          type="button"
          class="md-back-button"
          onclick={`window.location.href='${backNavigation.href}'`}
          aria-label={backNavigation.text}
          title={backNavigation.text}
        >
          <svg
            class="md-back-arrow"
            viewBox="0 0 24 24"
            fill="none"
            stroke="currentColor"
            aria-hidden="true"
          >
            <path
              stroke-linecap="round"
              stroke-linejoin="round"
              stroke-width="2"
              d="M15 18l-6-6 6-6"
            />
          </svg>
          <span class="md-back-text">Back</span>
        </button>
      )
    }

    <ol class="md-breadcrumb-list">
      {
        breadcrumbItems.map((item, index) => (
          <li class="md-breadcrumb-item">
            {item.href ? (
              <a href={item.href} class="md-breadcrumb-link">
                {item.name}
              </a>
            ) : (
              <span class="md-breadcrumb-current" aria-current="page">
                {item.name}
              </span>
            )}
            {index < breadcrumbItems.length - 1 && (
              <span class="md-breadcrumb-separator" aria-hidden="true">
                ›
              </span>
            )}
          </li>
        ))
      }
    </ol>
  </div>
</nav>

<style>
  .md-breadcrumb {
    margin-bottom: 2rem;
    max-width: 75rem;
    margin-left: auto;
    margin-right: auto;
    padding-left: 2rem;
    padding-right: 2rem;
    position: relative;
    z-index: 20;
  }

  .md-breadcrumb-container {
    display: flex;
    align-items: center;
    gap: 1rem;
    flex-wrap: wrap;
  }

  .md-back-button {
    display: inline-flex;
    align-items: center;
    gap: 0.75rem;
    padding: 0.75rem 1rem;
    background: #2a2a2a;
    border: none;
    border-radius: 6px;
    color: var(--md-primary, #34d399);
    font-size: 0.9rem;
    font-weight: 500;
    text-decoration: none;
    cursor: pointer;
    white-space: nowrap;
    min-height: 48px;
    touch-action: manipulation;
    -webkit-tap-highlight-color: transparent;
  }

  .md-back-button:focus-visible {
    outline: 2px solid var(--md-primary, #34d399);
    outline-offset: 2px;
  }

  .md-back-arrow {
    width: 1rem;
    height: 1rem;
    flex-shrink: 0;
  }

  .md-back-text {
    font-size: 0.9rem;
  }

  .md-breadcrumb-list {
    display: flex;
    gap: 0.5rem;
    list-style: none;
    padding: 0;
    margin: 0;
    color: rgba(255, 255, 255, 0.5);
    font-size: 0.9rem;
    flex-wrap: wrap;
    align-items: center;
    flex: 1;
  }

  .md-breadcrumb-item {
    display: flex;
    align-items: center;
    gap: 0.5rem;
  }

  .md-breadcrumb-link {
    color: var(--md-primary, #34d399);
    text-decoration: none;
    padding: 0.75rem;
    border-radius: 6px;
    min-height: 48px;
    display: flex;
    align-items: center;
    touch-action: manipulation;
    -webkit-tap-highlight-color: transparent;
  }

  .md-breadcrumb-current {
    color: #ffffff;
    font-weight: 600;
    padding: 0.75rem;
  }

  .md-breadcrumb-separator {
    color: rgba(255, 255, 255, 0.5);
    user-select: none;
    font-size: 1rem;
  }

  @media (max-width: 768px) {
    .md-breadcrumb {
      padding-left: 1rem;
      padding-right: 1rem;
      margin-bottom: 1.5rem;
    }

    .md-breadcrumb-container {
      gap: 1rem;
    }

    .md-back-button {
      padding: 0.75rem 1rem;
      font-size: 0.9rem;
      min-height: 48px;
    }

    .md-breadcrumb-list {
      font-size: 0.9rem;
    }
  }

  @media (max-width: 480px) {
    .md-breadcrumb {
      padding-left: 0.75rem;
      padding-right: 0.75rem;
      margin-bottom: 1rem;
    }

    .md-breadcrumb-container {
      gap: 0.75rem;
      flex-direction: column;
      align-items: flex-start;
    }

    .md-back-button {
      padding: 0.75rem 1rem;
      font-size: 0.85rem;
      min-height: 48px;
      width: 100%;
      justify-content: center;
    }

    .md-breadcrumb-list {
      font-size: 0.85rem;
      gap: 0.5rem;
      width: 100%;
    }

    .md-breadcrumb-item {
      gap: 0.5rem;
    }

    .md-breadcrumb-link,
    .md-breadcrumb-current {
      padding: 0.75rem;
      min-height: 48px;
    }
  }

  @media (prefers-contrast: high) {
    .md-back-button {
      border-color: currentColor;
    }

    .md-breadcrumb-link {
      text-decoration: underline;
    }
  }
</style>
