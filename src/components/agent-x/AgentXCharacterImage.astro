---
// Resolves an Agent X icon filename to an optimized <Image> via astro:assets glob.
import { Image } from 'astro:assets';

const images = import.meta.glob<{ default: ImageMetadata }>(
  '../../assets/images/games/agent-x/characters/**/*.png',
  { eager: true }
);

export interface Props {
  filename: string;
  alt?: string;
  class?: string;
  loading?: 'eager' | 'lazy';
}

function findImage(filename: string): ImageMetadata | null {
  const key = Object.keys(images).find(k => k.endsWith('/' + filename));
  return key ? images[key].default : null;
}

const { filename, alt = 'Agent', class: className = '', loading = 'lazy' } = Astro.props;
const imageData = findImage(filename);
---

{imageData ? (
  <Image src={imageData} alt={alt} class={className} loading={loading} format="webp" quality={95} />
) : (
  <div class={`ax-image-fallback ${className}`} style="background: var(--ax-bg-secondary); border-radius: 8px; display: flex; align-items: center; justify-content: center; color: var(--ax-text-muted); font-size: 0.75rem; min-height: 100px;">
    {alt}
  </div>
)}
