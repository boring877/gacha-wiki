---
import {
  generateBreadcrumbs,
  getParentBreadcrumb,
  generateBackNavigation,
} from '../utils/breadcrumb-generator.js';
import type { BreadcrumbItem, BackNavigation } from '../utils/breadcrumb-generator.js';

export interface Props {
  // Manual breadcrumb items (legacy support)
  items?: Array<{
    name: string;
    href?: string;
  }>;
  // Auto-generation options
  autoGenerate?: boolean;
  gameKey?: string;
  currentPageName?: string;
  includeHome?: boolean;
  showBackButton?: boolean;
}

const {
  items,
  autoGenerate = false,
  gameKey,
  currentPageName,
  includeHome = true,
  showBackButton = true,
} = Astro.props;

// Generate breadcrumbs automatically if requested, otherwise use provided items
let breadcrumbItems: BreadcrumbItem[] = items || [];
let backNavigation: BackNavigation | null = null;

if (autoGenerate) {
  breadcrumbItems = generateBreadcrumbs(Astro.url.pathname, {
    includeHome,
    currentPageName,
    gameKey,
  });

  if (showBackButton) {
    backNavigation = generateBackNavigation(Astro.url.pathname, { gameKey });
  }
} else if (showBackButton && items) {
  // Generate back navigation from provided items
  const parent = getParentBreadcrumb(items);
  if (parent) {
    backNavigation = {
      href: parent.href || '/',
      text: `Back to ${parent.name}`,
      parentName: parent.name,
    };
  }
}
---

<nav class="general-breadcrumb" aria-label="Breadcrumb">
  <div class="general-breadcrumb-container">
    {/* Back Button */}
    {
      backNavigation && (
        <button
          type="button"
          class="general-back-button"
          onclick={`window.location.href='${backNavigation.href}'`}
          aria-label={backNavigation.text}
          title={backNavigation.text}
        >
          <svg
            class="general-back-arrow"
            viewBox="0 0 24 24"
            fill="none"
            stroke="currentColor"
            aria-hidden="true"
          >
            <path
              stroke-linecap="round"
              stroke-linejoin="round"
              stroke-width="2"
              d="M15 18l-6-6 6-6"
            />
          </svg>
          <span class="general-back-text">Back</span>
        </button>
      )
    }

    {/* Breadcrumb Trail */}
    <ol class="general-breadcrumb-list">
      {
        breadcrumbItems.map((item, index) => (
          <li class="general-breadcrumb-item">
            {item.href ? (
              <a href={item.href} class="general-breadcrumb-link">
                {item.name}
              </a>
            ) : (
              <span class="general-breadcrumb-current" aria-current="page">
                {item.name}
              </span>
            )}
            {index < breadcrumbItems.length - 1 && (
              <span class="general-breadcrumb-separator" aria-hidden="true">
                ›
              </span>
            )}
          </li>
        ))
      }
    </ol>
  </div>
</nav>

<style>
  .general-breadcrumb {
    margin-bottom: 2rem;
    /* Align with parent container */
    max-width: 75rem;
    margin-left: auto;
    margin-right: auto;
    padding-left: 2rem;
    padding-right: 2rem;
    /* Ensure breadcrumb appears above other elements */
    position: relative;
    z-index: 20;
  }

  .general-breadcrumb-container {
    display: flex;
    align-items: center;
    gap: 1rem;
    flex-wrap: wrap;
  }

  /* Back Button Styles - General Theme */
  .general-back-button {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.5rem 1rem;
    background: rgba(255, 183, 77, 0.1);
    border: 1px solid rgba(255, 183, 77, 0.3);
    border-radius: 0.5rem;
    color: var(--amber-glow);
    font-size: 0.9rem;
    font-weight: 500;
    text-decoration: none;
    cursor: pointer;
    white-space: nowrap;
    /* Touch-friendly sizing - WCAG AA compliant */
    min-height: 44px;
    min-width: 88px;
    /* Touch optimization */
    touch-action: manipulation;
    -webkit-tap-highlight-color: rgba(255, 183, 77, 0.2);
  }

  .general-back-button:focus-visible {
    outline: 2px solid var(--amber-glow);
    outline-offset: 2px;
  }

  .general-back-arrow {
    width: 1rem;
    height: 1rem;
    flex-shrink: 0;
  }

  .general-back-text {
    font-size: 0.9rem;
  }

  /* Breadcrumb Trail Styles */
  .general-breadcrumb-list {
    display: flex;
    gap: 0.5rem;
    list-style: none;
    padding: 0;
    margin: 0;
    color: var(--text-secondary);
    font-size: 0.9rem;
    flex-wrap: wrap;
    align-items: center;
    flex: 1;
  }

  .general-breadcrumb-item {
    display: flex;
    align-items: center;
    gap: 0.5rem;
  }

  .general-breadcrumb-link {
    color: var(--amber-glow);
    text-decoration: none;
    opacity: 0.9;
    padding: 0.5rem 0.75rem;
    border-radius: 0.25rem;
    /* Touch-friendly link area */
    min-height: 44px;
    display: flex;
    align-items: center;
    touch-action: manipulation;
    -webkit-tap-highlight-color: rgba(255, 183, 77, 0.2);
  }

  .general-breadcrumb-current {
    color: var(--text-inverse);
    font-weight: 600;
    padding: 0.25rem 0.5rem;
  }

  .general-breadcrumb-separator {
    color: var(--neutral-400);
    user-select: none;
    opacity: 0.7;
    font-size: 1rem;
  }

  /* Mobile responsiveness - match Zone Nova layout breakpoints */
  @media (max-width: 768px) {
    .general-breadcrumb {
      padding-left: 1rem;
      padding-right: 1rem;
    }

    .general-breadcrumb-container {
      gap: 0.75rem;
    }

    .general-back-button {
      padding: 0.5rem 0.9rem;
      font-size: 0.85rem;
      min-height: 44px;
      min-width: 80px;
    }

    .general-back-text {
      font-size: 0.85rem;
    }

    .general-breadcrumb-list {
      font-size: 0.85rem;
    }
  }

  @media (max-width: 480px) {
    .general-breadcrumb {
      padding-left: 0.5rem;
      padding-right: 0.5rem;
    }

    .general-breadcrumb-container {
      gap: 0.5rem;
      flex-direction: row;
      align-items: center;
    }

    .general-back-button {
      padding: 0.5rem 0.8rem;
      font-size: 0.8rem;
      min-height: 44px;
      min-width: 72px;
    }

    .general-back-text {
      font-size: 0.8rem;
    }

    .general-breadcrumb-list {
      font-size: 0.8rem;
      gap: 0.3rem;
    }

    .general-breadcrumb-item {
      gap: 0.3rem;
    }

    .general-breadcrumb-link,
    .general-breadcrumb-current {
      padding: 0.5rem 0.6rem;
      min-height: 44px;
    }
  }

  /* High contrast mode support */
  @media (prefers-contrast: high) {
    .general-back-button {
      border-color: currentColor;
    }

    .general-breadcrumb-link {
      text-decoration: underline;
    }
  }
</style>
