---
// Landing layout for Desire Immortal Realm (mirrors make-drama's LandingLayout).
import DIRBaseHead from '../../components/desire-immortal-realm/DIRBaseHead.astro';
import GameSEO from '../../components/GameSEO.astro';
import Analytics from '../../components/Analytics.astro';
import Header from '../../components/Header.astro';
import Footer from '../../components/Footer.astro';
import GameNavigationHeader from '../../components/GameNavigationHeader.astro';
import MobileMenu from '../../components/MobileMenu.astro';
import CustomScrollbar from '../../components/CustomScrollbar.astro';
import DecorativeLines from '../../components/DecorativeLines.astro';
import Breadcrumb from '../../components/desire-immortal-realm/Breadcrumb.astro';
import { Image } from 'astro:assets';

import '../../styles/games/desire-immortal-realm/dir-colors.css';
import '../../styles/global.css';
import '../../styles/typography.css';
import '../../styles/games/desire-immortal-realm/dir-layout.css';
import '../../styles/games/desire-immortal-realm/dir-landing.css';

export interface Props {
  title: string;
  description?: string;
  gameTitle?: string;
  heroImage?: any;
}

const { title, description, gameTitle, heroImage } = Astro.props;
const seoTitle = `${title} | Desire Immortal Realm`;
const seoDescription = description || 'Complete guide for Desire Immortal Realm. Character database, stats, skills, and lore for the Chinese xianxia cultivation gacha.';
---

<html lang="en">
  <head>
    <DIRBaseHead title={seoTitle} description={seoDescription} />
    <GameSEO gameKey="desire-immortal-realm" title={seoTitle} description={seoDescription} />
    <Analytics />
  </head>
  <body class="dir-body">
    <CustomScrollbar />
    <div class="bg-layer dir-bg-primary"></div>
    <div class="bg-layer dir-bg-accent-1"></div>
    <div class="bg-layer dir-bg-accent-2"></div>
    <div class="bg-layer dir-bg-accent-3"></div>

    <div class="dir-content">
      <Header />
      <GameNavigationHeader currentPath={Astro.url.pathname} gameKey="desire-immortal-realm" />
      <MobileMenu />

      <div class="dir-container">
        <main class="dir-main" id="main-content">
          {
            heroImage && gameTitle && (
              <section class="hero-section">
                <div class="hero-image-wrapper">
                  <div class="hero-image-container">
                    <Image
                      src={heroImage}
                      alt={gameTitle}
                      class="hero-image"
                      width={1200}
                      height={400}
                      loading="eager"
                      decoding="async"
                    />
                    <div class="hero-overlay" />
                  </div>
                </div>
                <h1 class="hero-title">{gameTitle}</h1>
                <DecorativeLines />
              </section>
            )
          }

          {!heroImage && gameTitle && (
            <section class="dir-title-only-section">
              <h1 class="dir-title-only-heading">{gameTitle}</h1>
              <DecorativeLines />
            </section>
          )}

          <Breadcrumb autoGenerate={true} gameKey="desire-immortal-realm" showBackButton={true} />

          <div class="content-area">
            <slot />
          </div>
        </main>
      </div>

      <Footer />
    </div>
  </body>
</html>
