---
import BlogLandingLayout from '../../../../layouts/taimanin-squad/BlogLandingLayout.astro';
import { blogConfig } from '../../../../data/taimanin-squad/blog.js';

const { meta, posts } = blogConfig;

// Format date helper
const formatDate = (dateString: string) => {
  const date = new Date(dateString);
  return date.toLocaleDateString('en-US', {
    year: 'numeric',
    month: 'short',
    day: 'numeric',
  });
};
---

<BlogLandingLayout
  title={meta.title}
  description={meta.description}
  gameTitle={meta.gameTitle}
  showTitle={true}
>
  <div class="ts-blog-page-container">
    <section class="ts-blog-content-section">
      <h2 class="ts-blog-section-title">Latest Posts</h2>

      {
        posts.length > 0 ? (
          <div class="ts-blog-grid">
            {posts.map(post => (
              <a href={`/guides/taimanin-squad/blog/${post.slug}`} class="ts-blog-card">
                <div class="ts-blog-card-meta">
                  <span class="ts-blog-card-category">{post.category}</span>
                  <span class="ts-blog-card-date">{formatDate(post.publishDate)}</span>
                </div>
                <h3 class="ts-blog-card-title">{post.title}</h3>
                <p class="ts-blog-card-excerpt">{post.description}</p>
                <div class="ts-blog-card-author">By {post.author}</div>
              </a>
            ))}
          </div>
        ) : (
          <div class="ts-blog-empty">
            <h3>No posts yet</h3>
            <p>Check back soon for tips, strategies, and insights!</p>
          </div>
        )
      }
    </section>
  </div>
</BlogLandingLayout>
