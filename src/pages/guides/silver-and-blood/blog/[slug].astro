---
import BlogDetailLayout from '../../../../layouts/silver-and-blood/BlogDetailLayout.astro';
import { blogConfig } from '../../../../data/silver-and-blood/blog.js';

export function getStaticPaths() {
  return blogConfig.posts.map(post => ({
    params: { slug: post.slug },
    props: { post },
  }));
}

const { post } = Astro.props;
---

<BlogDetailLayout post={post} />
