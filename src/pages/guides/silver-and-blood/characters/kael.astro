---
import CharacterTemplate from '../../../../templates/silver-and-blood/CharacterTemplate.astro';
import { CHARACTERS_BY_SLUG } from '../../../../data/silver-and-blood/characters.js';

const character = CHARACTERS_BY_SLUG.get('kael');

if (!character) {
  throw new Error("Character 'kael' not found.");
}
---

<CharacterTemplate character={character} /> 