---
import MemoryTemplate from '../../../../templates/zone-nova/MemoryTemplate.astro';
import { gaiamemoryData } from '../../../../data/zone-nova/memories/gaiacard.js';

export const prerender = true;

// Memory-specific data
const memoryData = {
  ...gaiamemoryData,
};

// SEO data
const memorySEO = {
  title: 'A Smile That Topples Cities - Gaia Memory Card | Zone Nova | GachaWiki',
  description:
    'Complete guide to A Smile That Topples Cities memory card for Gaia in Zone Nova. SSR Holy memory with Max HP +40% and up to 80% healing output boost.',
};
---

<MemoryTemplate memoryData={memoryData} memorySEO={memorySEO} />
