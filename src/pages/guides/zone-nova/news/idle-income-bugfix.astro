---
import ZoneNovaLayout1 from '../../../../layouts/ZoneNovaLayout1.astro';
import FormattedDate from '../../../../components/FormattedDate.astro';
---

<ZoneNovaLayout1 
	title="Idle Income System Bug Fix & Compensation - Zone Nova - GachaWiki" 
	description="ZONE NOVA bug fix and compensation for idle income system anomaly"
	gameTitle="Bug Fix & Compensation"
	
	
>
	<Fragment slot="head">
		<style>
			.back-button {
				display: inline-flex;
				align-items: center;
				gap: 0.5rem;
				background: var(--bg-primary);
				color: var(--text-primary);
				text-decoration: none;
				padding: 1rem 1.5rem;
				border-radius: 12px;
				border: 1px solid var(--amber-glow);
				font-family: 'Space Grotesk', sans-serif;
				font-weight: 600;
				font-size: 1rem;
				margin-bottom: 2rem;
				transition: all 0.3s ease;
				box-shadow: 0 4px 12px rgba(164, 120, 100, 0.2);
			}

			.back-button:hover {
				background: var(--amber-glow);
				color: var(--text-inverse);
				transform: translateY(-2px);
				box-shadow: 0 8px 20px rgba(164, 120, 100, 0.3);
			}

			.back-button:hover .back-arrow {
				transform: translateX(-3px);
			}

			.back-arrow {
				font-size: 1.2rem;
				transition: transform 0.3s ease;
			}

			.character-page {
				background: var(--bg-secondary);
				min-height: 100vh;
				padding: 2rem 0;
			}

			.character-container {
				max-width: 1200px;
				margin: 0 auto;
				padding: 0 2rem;
			}

			.character-header {
				background: linear-gradient(135deg, var(--bg-primary), var(--bg-secondary));
				border-radius: 20px;
				padding: 3rem;
				margin-bottom: 3rem;
				border: 1px solid var(--amber-glow);
				box-shadow: 0 12px 30px rgba(164, 120, 100, 0.1);
				text-align: center;
				transition: all 0.4s ease;
			}

			.character-header:hover {
				transform: translateY(-4px);
				box-shadow: 0 16px 40px rgba(164, 120, 100, 0.15);
			}

			.character-title {
				font-family: 'Space Grotesk', sans-serif;
				font-size: 3rem;
				font-weight: 700;
				background: linear-gradient(135deg, var(--amber-glow), var(--teal-crystal));
				-webkit-background-clip: text;
				-webkit-text-fill-color: transparent;
				margin: 0 0 1rem 0;
				animation: titleGlow 3s ease-in-out infinite alternate;
			}

			.character-subtitle {
				color: var(--text-secondary);
				font-size: 1.2rem;
				margin: 0;
				font-weight: 500;
			}

			.content-section {
				background: var(--bg-primary);
				border-radius: 20px;
				padding: 3rem;
				margin-bottom: 2rem;
				border: 1px solid var(--neutral-200);
				box-shadow: 0 8px 25px rgba(164, 120, 100, 0.08);
				transition: all 0.4s ease;
				animation: sectionFadeIn 0.8s ease-out;
			}

			.content-section:hover {
				transform: translateY(-2px);
				box-shadow: 0 12px 35px rgba(164, 120, 100, 0.12);
				border-color: var(--teal-crystal);
			}

			.section-title {
				font-family: 'Space Grotesk', sans-serif;
				font-size: 2rem;
				font-weight: 700;
				color: var(--text-primary);
				margin: 0 0 1.5rem 0;
				padding-bottom: 0.75rem;
				border-bottom: 2px solid var(--amber-glow);
				position: relative;
			}

			.section-title::after {
				content: '';
				position: absolute;
				bottom: -2px;
				left: 0;
				width: 60px;
				height: 2px;
				background: var(--teal-crystal);
			}

			.subsection-title {
				font-family: 'Space Grotesk', sans-serif;
				font-size: 1.5rem;
				font-weight: 600;
				color: var(--text-primary);
				margin: 2rem 0 1rem 0;
				padding-left: 1rem;
				border-left: 4px solid var(--amber-glow);
			}

			.info-card {
				background: linear-gradient(135deg, rgba(255, 183, 77, 0.05), rgba(0, 172, 193, 0.05));
				border: 1px solid var(--neutral-200);
				border-radius: 15px;
				padding: 2rem;
				margin: 1.5rem 0;
				transition: all 0.3s ease;
				position: relative;
				overflow: hidden;
			}

			.info-card::before {
				content: '';
				position: absolute;
				top: 0;
				left: -100%;
				width: 100%;
				height: 100%;
				background: linear-gradient(90deg, transparent, rgba(255, 183, 77, 0.1), transparent);
				transition: left 0.6s ease;
			}

			.info-card:hover::before {
				left: 100%;
			}

			.info-card:hover {
				transform: translateY(-2px);
				box-shadow: 0 8px 25px rgba(164, 120, 100, 0.15);
				border-color: var(--teal-crystal);
			}

			.skill-card {
				background: linear-gradient(135deg, var(--bg-primary), var(--bg-secondary));
				border: 1px solid var(--neutral-200);
				border-radius: 15px;
				padding: 2rem;
				margin: 1rem 0;
				transition: all 0.3s ease;
				border-left: 4px solid var(--teal-crystal);
			}

			.skill-card:hover {
				transform: translateY(-2px);
				box-shadow: 0 8px 25px rgba(0, 172, 193, 0.15);
				border-left-color: var(--amber-glow);
			}

			.stats-grid {
				display: grid;
				grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
				gap: 1rem;
				margin: 1.5rem 0;
			}

			.stat-item {
				background: var(--bg-primary);
				padding: 1rem;
				border-radius: 10px;
				border: 1px solid var(--neutral-200);
				text-align: center;
				transition: all 0.3s ease;
			}

			.stat-item:hover {
				background: var(--neutral-50);
				transform: translateY(-2px);
				box-shadow: 0 6px 20px rgba(164, 120, 100, 0.1);
			}

			.stat-label {
				font-weight: 600;
				color: var(--amber-glow);
				font-size: 0.9rem;
				margin-bottom: 0.5rem;
			}

			.stat-value {
				font-size: 1.1rem;
				color: var(--text-primary);
				font-weight: 700;
			}

			.text-content {
				color: var(--text-primary);
				line-height: 1.7;
				font-size: 1rem;
			}

			.text-content p {
				margin: 1rem 0;
			}

			.text-content ul, .text-content ol {
				padding-left: 2rem;
				margin: 1rem 0;
			}

			.text-content li {
				margin: 0.5rem 0;
				line-height: 1.6;
			}

			.text-content strong {
				color: var(--amber-glow);
				font-weight: 600;
			}

			.highlight-text {
				background: linear-gradient(135deg, rgba(255, 183, 77, 0.1), rgba(0, 172, 193, 0.1));
				padding: 0.2rem 0.5rem;
				border-radius: 6px;
				font-weight: 600;
			}

			@keyframes sectionFadeIn {
				0% {
					opacity: 0;
					transform: translateY(20px);
				}
				100% {
					opacity: 1;
					transform: translateY(0);
				}
			}

			@keyframes titleGlow {
				0% {
					text-shadow: 
						0 0 10px rgba(255, 183, 77, 0.3),
						0 0 20px rgba(255, 183, 77, 0.2);
				}
				100% {
					text-shadow: 
						0 0 20px rgba(255, 183, 77, 0.6),
						0 0 30px rgba(255, 183, 77, 0.4);
				}
			}

			@media (max-width: 768px) {
				.character-container {
					padding: 0 1rem;
				}

				.character-header {
					padding: 2rem;
				}

				.character-title {
					font-size: 2rem;
				}

				.content-section {
					padding: 2rem;
				}

				.stats-grid {
					grid-template-columns: 1fr;
				}
			}
		</style>
	</Fragment>

	<div class="character-page">
		<div class="character-container">


			<!-- Character Header -->
			<div class="character-header">
				<h1 class="character-title">🛠️ Idle Income System Bug Fix & Compensation</h1>
				<p class="character-subtitle">Official ZONE NOVA Announcement</p>
			</div>

			<!-- Announcement Details -->
			<section class="content-section">
				<h2 class="section-title">📢 Announcement Details</h2>
				<div class="stats-grid">
					<div class="stat-item">
						<div class="stat-label">Issue</div>
						<div class="stat-value">Idle Income Anomaly</div>
					</div>
					<div class="stat-item">
						<div class="stat-label">Action</div>
						<div class="stat-value">Bug Fix & Compensation</div>
					</div>
					<div class="stat-item">
						<div class="stat-label">Status</div>
						<div class="stat-value">✅ Resolved</div>
					</div>
				</div>
			</section>

			<!-- Official Message -->
			<section class="content-section">
				<h2 class="section-title">💌 Official Message</h2>
				<div class="skill-card">
					<div class="text-content">
						<p><strong>Dear Captains:</strong></p>
						<p>Thank you all for your continued enthusiastic support and companionship for ZONE NOVA! Last night we discovered that some captains abnormally obtained extra Energy Supplements due to an idle income system anomaly. To ensure the stability and fairness of the game's ecosystem, the development team will carry out the following handling.</p>
					</div>
				</div>
			</section>

			<!-- Bug Fix Details -->
			<section class="content-section">
				<h2 class="section-title">🔧 Bug Fix Details</h2>
				<div class="info-card">
					<div class="text-content">
						<p><strong>Handling Method:</strong> We will reclaim from captains who obtained abnormal Energy Supplements due to the idle income anomaly.</p>
						<p><strong>Affected Scope:</strong> Only accounts that abnormally obtained Energy Supplements will be processed. Unaffected captains need not worry.</p>
					</div>
				</div>
			</section>

			<!-- Compensation for All Players -->
			<section class="content-section">
				<h2 class="section-title">🎁 Compensation for All Players</h2>
				
				<h3 class="subsection-title">Compensation Items</h3>
				<div class="stats-grid">
					<div class="stat-item">
						<div class="stat-label">Item</div>
						<div class="stat-value">Energy Supplement</div>
					</div>
					<div class="stat-item">
						<div class="stat-label">Quantity</div>
						<div class="stat-value">×5</div>
					</div>
				</div>

				<h3 class="subsection-title">Eligibility & Distribution</h3>
				<div class="info-card">
					<div class="text-content">
						<p><strong>📅 Eligibility:</strong> Captains who have logged into the game before June 7, 2025 23:59</p>
						<p><strong>📬 Distribution Method:</strong> Compensation will be distributed through in-game mail</p>
					</div>
				</div>
			</section>

			<!-- Source Information -->
			<section class="content-section">
				<h2 class="section-title">📝 Source Information</h2>
				<div class="info-card">
					<div class="text-content">
						<p style="text-align: center; color: var(--text-secondary); font-style: italic;">Source: Official ZONE NOVA announcement</p>
					</div>
				</div>
			</section>
		</div>
	</div>
</ZoneNovaLayout1> 