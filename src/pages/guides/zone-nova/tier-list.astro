---
import TierListLayout from '../../../layouts/zone-nova/TierListLayout.astro';
import { ZONE_NOVA_CHARACTERS } from '../../../data/zone-nova/characters.js';
import {
  ALL_TIER_LISTS,
  TIER_DEFINITIONS,
  TIER_ORDER,
  getGroupedTierSections,
  getRoleHighlights,
} from '../../../data/zone-nova/tier-lists.js';
import { ZNCharacterImage, ZNClassImage } from '../../../components/zone-nova/index.astro';

const characterMap = {};
ZONE_NOVA_CHARACTERS.forEach(char => {
  characterMap[char.name] = char;
});

function getTierClassName(tier: string): string {
  return tier.toLowerCase().replace('+', '-plus').replace(/\s+/g, '-');
}

function getListModeLabel(listType: string): string {
  if (listType === 'pvp') return 'Arena PvP';
  if (listType === 'new-player') return 'New Player / F2P';
  return 'PvE';
}

function formatCharacterNames(characters: { name: string }[], limit = 3): string {
  const names = characters.map(character => character.name);
  if (names.length <= limit) return names.join(', ');
  return `${names.slice(0, limit).join(', ')} +${names.length - limit} more`;
}

const structuredListTypes = ['pve', 'pvp', 'new-player'];

const structuredTierViews: Record<string, { roleHighlights: ReturnType<typeof getRoleHighlights>; classSections: ReturnType<typeof getGroupedTierSections> }> = {};
structuredListTypes.forEach(listType => {
  structuredTierViews[listType] = {
    roleHighlights: getRoleHighlights(listType),
    classSections: getGroupedTierSections(listType, 'class'),
  };
});
---

<TierListLayout
  title="Zone Nova Tier List - Character Rankings & Meta Guide"
  description="Comprehensive Zone Nova rankings for PvE, Arena PvP, and new player progression. Compare the best healers, rogues, buffers, and other classes."
>
  <div class="tier-list-container">
    <!-- Mode Toggle -->
    <div class="tl-mode-toggle">
      <button class="tl-mode-btn active" id="view-mode-btn">View Tier Lists</button>
      <button class="tl-mode-btn" id="maker-mode-btn">Create Your Own</button>
    </div>

    <!-- View Mode Content -->
    <div id="view-mode-content">
      <!-- List Type Selector -->
      <div class="tl-type-selector">
        <button class="tl-type-btn active" data-list-type="pve">PvE Rankings</button>
        <button class="tl-type-btn" data-list-type="pvp">PvP Arena</button>
        <button class="tl-type-btn" data-list-type="new-player">New Player / F2P</button>
      </div>

      <!-- Tier List Info -->
      <div class="tl-info" id="tier-list-info">
        <h2 id="list-title">{ALL_TIER_LISTS.pve.name}</h2>
        <p id="list-description">{ALL_TIER_LISTS.pve.description}</p>
        <p class="tl-updated" id="list-updated">Last updated: {ALL_TIER_LISTS.pve.lastUpdated}</p>
      </div>

      <!-- Structured Tier Displays -->
      {structuredListTypes.map(listType => (
        <div
          class="tl-structured-display"
          id={`${listType}-tier-display`}
          style={listType === 'pve' ? undefined : 'display: none;'}
        >
          <!-- Class Rankings -->
          <div class="tl-class-sections">
              {structuredTierViews[listType].classSections.map(section => (
                <section class="tl-class-section">
                  <div class="tl-class-header">
                    <div class="tl-class-title-row">
                      <ZNClassImage className={section.label} alt={section.label} size="medium" class="tl-class-icon" />
                      <div>
                        <h4 class="tl-class-name">{section.label}</h4>
                        <p class="tl-class-desc">
                          Best {section.label.toLowerCase()} picks for {getListModeLabel(listType)}.
                        </p>
                      </div>
                    </div>
                  </div>

                  {TIER_ORDER.map(tier =>
                    section.tiers[tier].length ? (
                      <div class="tl-tier-row">
                        <div class={`tl-tier-label tl-tier-${getTierClassName(tier)}`}>
                          {TIER_DEFINITIONS[tier].label}
                        </div>
                        <div class="tl-tier-chars">
                          {section.tiers[tier].map(charInfo => {
                            const charData = characterMap[charInfo.name];
                            if (!charData) return null;

                            return (
                              <div
                                class="tl-char-card"
                                onclick={`window.location.href=${JSON.stringify(charData.detailUrl)}`}
                              >
                                <div class="tl-char-img">
                                  <ZNCharacterImage
                                    imageName={charData.image}
                                    alt={charData.name}
                                    width={300}
                                    height={300}
                                    loading="lazy"
                                    quality={100}
                                    format="jpg"
                                  />
                                </div>
                                <div class="tl-char-name">{charData.name}</div>
                                <div class="tl-char-badges">
                                  <span class={`tl-badge tl-badge-rarity-${charData.rarity.toLowerCase()}`}>
                                    {charData.rarity}
                                  </span>
                                  {charData.element && (
                                    <span class={`tl-badge tl-badge-element-${charData.element.toLowerCase()}`}>
                                      {charData.element}
                                    </span>
                                  )}
                                </div>
                              </div>
                            );
                          })}
                        </div>
                      </div>
                    ) : null
                  )}
                </section>
              ))}
            </div>
          </div>
        </div>
      ))}
    </div>

    <!-- Maker Mode Content -->
    <div id="maker-mode-content" style="display: none;">
      <p class="tl-maker-desc">
        Click a character to select, then click a tier to place it. Or drag and drop.
      </p>
      <div class="tl-maker-controls">
        <button class="tl-control-btn" id="reset-tier-list">Reset Tier List</button>
      </div>

      <!-- Character Pool -->
      <div id="maker-pool">
        <div class="tl-maker-pool-grid">
          {ZONE_NOVA_CHARACTERS.map(char => (
            <div
              class="tl-pool-card draggable"
              data-character={char.name}
              data-rarity={char.rarity}
              data-element={char.element}
              draggable="true"
            >
              <div class="tl-pool-img">
                <ZNCharacterImage
                  imageName={char.image}
                  alt={char.name}
                  width={75}
                  height={75}
                  loading="lazy"
                  quality={90}
                  format="webp"
                />
              </div>
              <div class="tl-pool-name">{char.name}</div>
              <div class="tl-pool-badges">
                <span class={`tl-badge tl-badge-rarity-${char.rarity.toLowerCase()}`}>
                  {char.rarity}
                </span>
                {char.element && (
                  <span class={`tl-badge tl-badge-element-${char.element.toLowerCase()}`}>
                    {char.element}
                  </span>
                )}
              </div>
            </div>
          ))}
        </div>
      </div>

      <!-- Maker Tier List -->
      <div id="maker-tier-list">
        {Object.entries(TIER_DEFINITIONS).map(([tier, definition]) => (
          <div class="tl-tier-row tl-maker-row">
            <div class={`tl-tier-label tl-tier-${getTierClassName(tier)}`}>{definition.label}</div>
            <div class="tl-tier-chars tl-droppable" data-tier={tier}>
              <div class="tl-drop-hint">Drop characters here</div>
            </div>
          </div>
        ))}
      </div>
    </div>

  </div>

  <script define:vars={{ characterMap, ALL_TIER_LISTS }}>
    document.addEventListener('DOMContentLoaded', () => {
      const listTitle = document.getElementById('list-title');
      const listDescription = document.getElementById('list-description');
      const listUpdated = document.getElementById('list-updated');
      const typeButtons = document.querySelectorAll('.tl-type-btn');
      const viewModeBtn = document.getElementById('view-mode-btn');
      const makerModeBtn = document.getElementById('maker-mode-btn');
      const viewModeContent = document.getElementById('view-mode-content');
      const makerModeContent = document.getElementById('maker-mode-content');
      const tierListInfo = document.getElementById('tier-list-info');
      const tierLegend = document.getElementById('tier-legend');
      const tierListDisplays = document.querySelectorAll('.tl-structured-display');

      const makerPool = document.getElementById('maker-pool');
      const makerTierList = document.getElementById('maker-tier-list');
      const resetBtn = document.getElementById('reset-tier-list');

      let currentListType = 'pve';
      let currentMode = 'view';
      let selectedCharacter = null;
      const tierAssignments = {};
      const STORAGE_KEY = 'zn-custom-tier-list';
      let makerInitialized = false;

      function updateTierListInfo(listType) {
        const tierList = ALL_TIER_LISTS[listType];
        if (!tierList) return;
        if (listTitle) listTitle.textContent = tierList.name;
        if (listDescription) listDescription.textContent = tierList.description;
        if (listUpdated) listUpdated.textContent = `Last updated: ${tierList.lastUpdated}`;

        tierListDisplays.forEach(display => {
          display.style.display = display.id === `${listType}-tier-display` ? 'block' : 'none';
        });
      }

      typeButtons.forEach(button => {
        button.addEventListener('click', () => {
          const listType = button.dataset.listType;
          if (!listType || listType === currentListType) return;
          typeButtons.forEach(btn => btn.classList.remove('active'));
          button.classList.add('active');
          currentListType = listType;
          updateTierListInfo(listType);
        });
      });

      viewModeBtn.addEventListener('click', () => {
        currentMode = 'view';
        viewModeBtn.classList.add('active');
        makerModeBtn.classList.remove('active');
        viewModeContent.style.display = 'block';
        makerModeContent.style.display = 'none';
        tierListInfo.style.display = 'block';
        if (tierLegend) tierLegend.style.display = 'grid';
        if (makerPool) makerPool.style.display = 'none';
        if (makerTierList) makerTierList.style.display = 'none';
        updateTierListInfo(currentListType);
      });

      makerModeBtn.addEventListener('click', () => {
        currentMode = 'maker';
        makerModeBtn.classList.add('active');
        viewModeBtn.classList.remove('active');
        viewModeContent.style.display = 'none';
        makerModeContent.style.display = 'block';
        tierListInfo.style.display = 'none';
        if (tierLegend) tierLegend.style.display = 'none';
        if (makerPool) makerPool.style.display = 'block';
        if (makerTierList) makerTierList.style.display = 'flex';
        tierListDisplays.forEach(d => d.style.display = 'none');
        if (!makerInitialized) {
          initializeMaker();
          makerInitialized = true;
        }
      });

      function initializeMaker() {
        setupPoolCards();
        setupDropZones();
        loadFromStorage();
      }

      function setupPoolCards() {
        document.querySelectorAll('#maker-pool .tl-pool-card').forEach(card => {
          const characterName = card.dataset.character;

          card.addEventListener('dragstart', e => {
            e.dataTransfer.effectAllowed = 'move';
            e.dataTransfer.setData('character', characterName);
            e.dataTransfer.setData('source', 'pool');
            card.classList.add('dragging');
          });

          card.addEventListener('dragend', () => {
            card.classList.remove('dragging');
          });

          card.addEventListener('click', () => {
            if (selectedCharacter === characterName) {
              card.classList.remove('selected');
              selectedCharacter = null;
              document.querySelectorAll('.tl-droppable').forEach(z => z.classList.remove('awaiting-placement'));
              return;
            }

            document.querySelectorAll('.tl-pool-card').forEach(c => c.classList.remove('selected'));
            document.querySelectorAll('.tl-char-card.in-tier').forEach(c => c.classList.remove('selected'));
            card.classList.add('selected');
            selectedCharacter = characterName;
            document.querySelectorAll('.tl-droppable').forEach(z => z.classList.add('awaiting-placement'));
          });
        });
      }

      function setupDropZones() {
        document.querySelectorAll('.tl-droppable').forEach(zone => {
          const tierName = zone.dataset.tier;

          zone.addEventListener('dragover', e => {
            e.preventDefault();
            e.dataTransfer.dropEffect = 'move';
            zone.classList.add('drag-over');
          });

          zone.addEventListener('dragleave', () => {
            zone.classList.remove('drag-over');
          });

          zone.addEventListener('drop', e => {
            e.preventDefault();
            zone.classList.remove('drag-over');
            const characterName = e.dataTransfer.getData('character');
            const source = e.dataTransfer.getData('source');

            if (characterName) {
              if (source === 'tier' && tierAssignments[characterName]) {
                moveCardBetweenTiers(characterName, tierName);
              } else {
                moveCardToTier(characterName, tierName);
              }
            }
          });

          zone.addEventListener('click', () => {
            if (!selectedCharacter) return;
            moveCardToTier(selectedCharacter, tierName);
            document.querySelectorAll('.tl-pool-card').forEach(c => c.classList.remove('selected'));
            document.querySelectorAll('.tl-char-card.in-tier').forEach(c => c.classList.remove('selected'));
            document.querySelectorAll('.tl-droppable').forEach(z => z.classList.remove('awaiting-placement'));
            selectedCharacter = null;
          });
        });
      }

      function moveCardToTier(characterName, tierName) {
        const poolCard = document.querySelector(`#maker-pool [data-character="${characterName}"]`);
        const targetZone = document.querySelector(`#maker-tier-list [data-tier="${tierName}"]`);

        if (!poolCard || !targetZone) return;

        poolCard.style.display = 'none';

        const charData = characterMap[characterName];
        const tierCard = document.createElement('div');
        tierCard.className = 'tl-char-card draggable in-tier';
        tierCard.dataset.character = characterName;
        tierCard.draggable = true;

        const elementClass = charData?.element ? charData.element.toLowerCase() : '';
        const elementName = charData?.element
          ? charData.element.charAt(0).toUpperCase() + charData.element.slice(1).toLowerCase()
          : '';

        tierCard.innerHTML = `
          <div class="tl-char-img">
            <img src="${poolCard.querySelector('img').src}" alt="${characterName}" loading="lazy" />
          </div>
          <div class="tl-char-name">${characterName}</div>
          <div class="tl-char-badges">
            <span class="tl-badge tl-badge-rarity-${charData?.rarity?.toLowerCase() || 'sr'}">${charData?.rarity || 'SR'}</span>
            ${charData?.element ? `<span class="tl-badge tl-badge-element-${elementClass}">${elementName}</span>` : ''}
          </div>
          <button class="tl-remove-btn" title="Remove from tier">&times;</button>
        `;

        setupTierCardEvents(tierCard, characterName);

        const hint = targetZone.querySelector('.tl-drop-hint');
        if (hint) hint.style.display = 'none';

        targetZone.appendChild(tierCard);
        tierAssignments[characterName] = tierName;
        saveToStorage();
      }

      function setupTierCardEvents(card, characterName) {
        card.addEventListener('dragstart', e => {
          e.dataTransfer.effectAllowed = 'move';
          e.dataTransfer.setData('character', characterName);
          e.dataTransfer.setData('source', 'tier');
          card.classList.add('dragging');
        });

        card.addEventListener('dragend', () => {
          card.classList.remove('dragging');
        });

        card.querySelector('.tl-remove-btn').addEventListener('click', e => {
          e.stopPropagation();
          returnCardToPool(characterName);
        });
      }

      function moveCardBetweenTiers(characterName, newTierName) {
        const tierCard = document.querySelector(`#maker-tier-list .in-tier[data-character="${characterName}"]`);
        const targetZone = document.querySelector(`#maker-tier-list [data-tier="${newTierName}"]`);

        if (!tierCard || !targetZone) return;

        const oldZone = tierCard.parentElement;
        tierCard.remove();
        targetZone.appendChild(tierCard);
        tierAssignments[characterName] = newTierName;

        if (oldZone && oldZone.querySelectorAll('.in-tier').length === 0) {
          const hint = oldZone.querySelector('.tl-drop-hint');
          if (hint) hint.style.display = 'block';
        }

        const hint = targetZone.querySelector('.tl-drop-hint');
        if (hint) hint.style.display = 'none';

        saveToStorage();
      }

      function returnCardToPool(characterName) {
        const poolCard = document.querySelector(`#maker-pool [data-character="${characterName}"]`);
        const tierCard = document.querySelector(`#maker-tier-list .in-tier[data-character="${characterName}"]`);

        if (!poolCard || !tierCard) return;

        const oldZone = tierCard.parentElement;
        poolCard.style.display = '';
        tierCard.remove();
        delete tierAssignments[characterName];

        if (oldZone && oldZone.querySelectorAll('.in-tier').length === 0) {
          const hint = oldZone.querySelector('.tl-drop-hint');
          if (hint) hint.style.display = 'block';
        }

        saveToStorage();
      }

      resetBtn?.addEventListener('click', () => {
        Object.keys(tierAssignments).forEach(returnCardToPool);
        localStorage.removeItem(STORAGE_KEY);
      });

      function saveToStorage() {
        localStorage.setItem(STORAGE_KEY, JSON.stringify(tierAssignments));
      }

      function loadFromStorage() {
        const stored = localStorage.getItem(STORAGE_KEY);
        if (!stored) return;

        try {
          const data = JSON.parse(stored);
          Object.entries(data).forEach(([name, tier]) => {
            if (characterMap[name]) {
              moveCardToTier(name, tier);
            }
          });
        } catch (e) {
          console.error('Failed to load tier list:', e);
        }
      }

      updateTierListInfo('pve');
    });
  </script>
</TierListLayout>
