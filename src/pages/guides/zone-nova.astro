---
import ZoneNovaLayout from '../../layouts/ZoneNovaLayout.astro';
import FormattedDate from '../../components/FormattedDate.astro';
---

<ZoneNovaLayout 
	title="Zone Nova - GachaWiki" 
	description="Explore Zone Nova gacha game guides and content"
	gameTitle="Zone Nova"
	heroImage="/images/zonenova1.jpg"
>
	<Fragment slot="head">
		<style>
			.back-button {
				display: inline-flex;
				align-items: center;
				gap: 0.5rem;
				background: var(--bg-primary);
				color: var(--text-primary);
				text-decoration: none;
				padding: 1rem 1.5rem;
				border-radius: 12px;
				border: 1px solid var(--amber-glow);
				font-family: 'Space Grotesk', sans-serif;
				font-weight: 600;
				font-size: 1rem;
				margin-bottom: 2rem;
				transition: all 0.3s ease;
				box-shadow: 0 4px 12px rgba(164, 120, 100, 0.2);
			}

			.back-button:hover {
				background: var(--amber-glow);
				color: var(--text-inverse);
				transform: translateY(-2px);
				box-shadow: 0 8px 20px rgba(164, 120, 100, 0.3);
			}

			.back-button:hover .back-arrow {
				transform: translateX(-3px);
			}

			.back-arrow {
				font-size: 1.2rem;
				transition: transform 0.3s ease;
			}

			.content-section {
				background: var(--bg-secondary);
				padding: 3rem;
				border-radius: 20px;
				margin-bottom: 3rem;
				border: 1px solid var(--amber-glow);
				box-shadow: 0 12px 30px rgba(164, 120, 100, 0.2);
				transition: all 0.4s ease;
				animation: sectionFadeIn 0.8s ease-out;
			}

			.content-section:hover {
				transform: translateY(-4px);
				box-shadow: 0 16px 40px rgba(164, 120, 100, 0.3);
				border-color: var(--teal-crystal);
			}
			
			.section-title {
				font-family: 'Space Grotesk', sans-serif;
				font-size: 2.5rem;
				font-weight: 700;
				background: linear-gradient(135deg, var(--amber-glow), var(--teal-crystal));
				-webkit-background-clip: text;
				-webkit-text-fill-color: transparent;
				margin: 0 0 1.5rem 0;
				text-align: center;
				animation: titleGlow 3s ease-in-out infinite alternate;
			}

			.game-description {
				color: var(--text-secondary);
				font-size: 1.2rem;
				line-height: 1.7;
				margin-bottom: 2rem;
				text-align: center;
				max-width: 60rem;
				margin-left: auto;
				margin-right: auto;
			}

			.last-updated {
				color: var(--amber-glow);
				font-size: 1rem;
				text-align: center;
				font-weight: 500;
				opacity: 0.9;
			}
			
			.guides-grid {
				display: grid;
				grid-template-columns: repeat(auto-fit, minmax(350px, 1fr));
				gap: 2rem;
				margin-top: 3rem;
			}
			
			.guide-card {
				background: var(--bg-primary);
				padding: 2.5rem;
				border-radius: 16px;
				border: 1px solid var(--mocha-mousse);
				transition: all 0.4s ease;
				cursor: pointer;
				position: relative;
				overflow: hidden;
				animation: cardSlideUp 0.6s ease-out;
				height: 100%;
			}

			.guide-card::before {
				content: '';
				position: absolute;
				top: 0;
				left: -100%;
				width: 100%;
				height: 100%;
				background: linear-gradient(90deg, transparent, rgba(255, 183, 77, 0.1), transparent);
				transition: left 0.6s ease;
			}

			.guide-card:hover::before {
				left: 100%;
			}

			.guide-card:hover {
				transform: translateY(-8px) scale(1.02);
				box-shadow: 0 20px 40px rgba(0, 172, 193, 0.3);
				border-color: var(--teal-crystal);
				background: var(--bg-secondary);
			}
			
			.guide-card h3 {
				font-family: 'Space Grotesk', sans-serif;
				color: var(--text-primary);
				margin: 0 0 1rem 0;
				font-size: 1.6rem;
				font-weight: 600;
				transition: all 0.3s ease;
				display: flex;
				align-items: center;
				gap: 0.75rem;
			}

			.guide-card:hover h3 {
				color: var(--amber-glow);
				transform: translateX(5px);
			}

			.guide-card p {
				margin: 0;
				line-height: 1.6;
				color: var(--text-secondary);
				font-size: 1.1rem;
				transition: color 0.3s ease;
			}

			.guide-card:hover p {
				color: var(--text-primary);
			}
			
			.guide-card-link {
				text-decoration: none;
				color: inherit;
				display: block;
			}

			.guide-emoji {
				font-size: 2rem;
				transition: transform 0.3s ease;
			}

			.guide-card:hover .guide-emoji {
				transform: scale(1.2) rotate(5deg);
			}

			@keyframes sectionFadeIn {
				0% {
					opacity: 0;
					transform: translateY(20px);
				}
				100% {
					opacity: 1;
					transform: translateY(0);
				}
			}

			@keyframes cardSlideUp {
				0% {
					opacity: 0;
					transform: translateY(30px);
				}
				100% {
					opacity: 1;
					transform: translateY(0);
				}
			}

			@keyframes playButtonGlow {
				0% {
					box-shadow: 0 8px 20px rgba(255, 183, 77, 0.4);
					filter: brightness(1);
				}
				100% {
					box-shadow: 0 8px 20px rgba(255, 183, 77, 0.6), 0 0 20px rgba(0, 172, 193, 0.3);
					filter: brightness(1.1);
				}
			}

			@media (max-width: 768px) {
				.guides-grid {
					grid-template-columns: 1fr;
					gap: 1.5rem;
				}
				
				.content-section {
					padding: 2rem;
					margin-bottom: 2rem;
				}
				
				.section-title {
					font-size: 2rem;
				}
			}
		</style>
	</Fragment>

	<!-- Back Button -->
	<a href="/guides/" class="back-button">
		<span class="back-arrow">←</span>
		Back to Guides
	</a>

	<!-- Game Overview Section -->
	<section class="content-section">
		<h2 class="section-title">About</h2>
		<div class="game-description">
			In the year 20XX, mysterious black spheres suddenly appeared hovering above cities worldwide, drawing the attention of all nations. However, these spheres instantly expanded into massive black holes, crashing down and destroying everything in their path.
		</div>
		
		<!-- Play Game Button -->
		<div style="text-align: center; margin: 2rem 0;">
			<a href="https://game.erolabsshare.net/app/f302f717/Zone_Nova" target="_blank" style="display: inline-flex; align-items: center; gap: 0.75rem; background: linear-gradient(135deg, var(--amber-glow), var(--teal-crystal)); color: white; padding: 1rem 2.5rem; border-radius: 25px; font-family: 'Space Grotesk', sans-serif; font-size: 1.2rem; font-weight: 600; text-decoration: none; box-shadow: 0 8px 20px rgba(255, 183, 77, 0.4); transition: all 0.3s ease; animation: playButtonGlow 2s ease-in-out infinite alternate;" onmouseover="this.style.transform='translateY(-3px) scale(1.05)'; this.style.boxShadow='0 12px 30px rgba(255, 183, 77, 0.6)'" onmouseout="this.style.transform='translateY(0) scale(1)'; this.style.boxShadow='0 8px 20px rgba(255, 183, 77, 0.4)'">
				<span style="font-size: 1.5rem;">🎮</span>
				Play Zone Nova
				<span style="font-size: 1.2rem;">→</span>
			</a>
		</div>
		
		<p class="last-updated">
			Last Updated: <FormattedDate date={new Date()} />
		</p>
	</section>

	<!-- Guides Section -->
	<section class="content-section">
		<h2 class="section-title">Game Guides</h2>
		
		<div class="guides-grid">
			<a href="/guides/zone-nova/updates/" class="guide-card-link">
				<div class="guide-card">
					<h3><span class="guide-emoji">🔧</span> News & Maintenance</h3>
					<p>Patch notes, maintenance, and announcements.</p>
				</div>
			</a>
			
			<a href="/guides/zone-nova/events/" class="guide-card-link">
				<div class="guide-card">
					<h3><span class="guide-emoji">🎉</span> Events</h3>
					<p>Current events, limited-time shops.</p>
				</div>
			</a>
			
			<a href="/guides/zone-nova/characters/" class="guide-card-link">
				<div class="guide-card">
					<h3><span class="guide-emoji">⚔️</span> Characters</h3>
					<p>Character guides, skills Information, and more !.</p>
				</div>
			</a>
			
			<a href="/guides/zone-nova/runes/" class="guide-card-link">
				<div class="guide-card">
					<h3><span class="guide-emoji">🔮</span> Runes</h3>
					<p>All runes stats and sub stats, and items set stats.</p>
				</div>
			</a>

			<a href="/guides/zone-nova/crafting/" class="guide-card-link">
				<div class="guide-card">
					<h3><span class="guide-emoji">🔨</span> Crafting</h3>
					<p>Where to craft items and how to craft them.</p>
				</div>
			</a>

			<a href="/guides/zone-nova/contributing/" class="guide-card-link">
				<div class="guide-card">
					<h3><span class="guide-emoji">📝</span> Contributing</h3>
					<p>Learn how to contribute to the Zone Nova guides.</p>
				</div>
			</a>
		</div>
	</section>
</ZoneNovaLayout>