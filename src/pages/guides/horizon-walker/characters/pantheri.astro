---
import CharacterIndividualLayout from '../../../../layouts/horizon-walker/CharacterIndividualLayout.astro';
import CharacterPageTemplate from '../../../../templates/horizon-walker/CharacterPageTemplate.astro';
import {
  pantheriData,
  pantheriSEO,
  pantheriHighlighting,
} from '../../../../data/horizon-walker/characters/pantheri.js';

export const prerender = true;
---

<CharacterIndividualLayout
  title={pantheriSEO.title}
  description={pantheriSEO.description}
  gameTitle={pantheriData.name}
>
  <CharacterPageTemplate
    characterData={{ ...pantheriData, highlightPatterns: pantheriHighlighting }}
  />
</CharacterIndividualLayout>
