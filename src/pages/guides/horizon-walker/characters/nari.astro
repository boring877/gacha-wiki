---
import CharacterIndividualLayout from '../../../../layouts/horizon-walker/CharacterIndividualLayout.astro';
import CharacterPageTemplate from '../../../../templates/horizon-walker/CharacterPageTemplate.astro';
import {
  nari,
  nariSEO,
  nariHighlighting,
} from '../../../../data/horizon-walker/characters/nari.js';

export const prerender = true;
---

<CharacterIndividualLayout
  title={nariSEO.title}
  description={nariSEO.description}
  gameTitle={nari.name}
>
  <CharacterPageTemplate characterData={{ ...nari, highlightPatterns: nariHighlighting }} />
</CharacterIndividualLayout>
