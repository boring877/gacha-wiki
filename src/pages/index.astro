---
import HomeLayout from '../layouts/HomeLayout.astro';
import { SITE_TITLE, SITE_DESCRIPTION } from '../consts';
---

<HomeLayout title={SITE_TITLE} description={SITE_DESCRIPTION}>
	<!-- Critical resource hints -->
	<link rel="preload" href="/images/zonenova.jpg" as="image" type="image/jpeg" />
	<link rel="dns-prefetch" href="https://fonts.googleapis.com" />
	<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
	
	<!-- Main Content Area -->
	<main style="max-width: 50rem; margin: 0 auto; padding: 8rem 2rem 4rem 2rem; min-height: 100vh; contain: layout style;">
		<!-- Games Section - Option 5: Featured Showcase Style -->
		<section style="margin-bottom: 6rem; contain: layout style;">
			<div style="text-align: center; margin-bottom: 3rem;">
				<h2 style="font-family: 'Space Grotesk', sans-serif; font-size: clamp(2.5rem, 4vw, 3.5rem); font-weight: 700; color: var(--text-primary); background: linear-gradient(135deg, var(--amber-glow), var(--teal-crystal)); -webkit-background-clip: text; -webkit-text-fill-color: transparent; margin-bottom: 0.5rem; will-change: transform; transform: translateZ(0); contain: layout style paint;" class="title-hover">
					Game Guide Library
				</h2>
			</div>

			<!-- Featured Game - Zone Nova -->
			<div style="margin-bottom: 3rem;">
				<div style="background: var(--bg-primary); border-radius: 20px; overflow: hidden; border: 1px solid var(--amber-glow); box-shadow: 0 12px 30px rgba(164, 120, 100, 0.2); will-change: transform, box-shadow; transform: translateZ(0); contain: layout style paint;" class="featured-card">
					<a href="/guides/zone-nova/" style="text-decoration: none; color: inherit; display: block;">
						
						<!-- Featured Header -->
						<div style="background: linear-gradient(135deg, var(--amber-glow), var(--teal-crystal)); padding: 1rem 2rem; text-align: center; min-height: 56px; display: flex; align-items: center; justify-content: center;">
							<span style="color: white; font-family: 'Space Grotesk', sans-serif; font-size: 0.9rem; font-weight: 600; display: flex; align-items: center; justify-content: center; gap: 0.5rem;">
								<span style="font-size: 1.2rem;" aria-hidden="true">⭐</span>
								FEATURED GAME
							</span>
						</div>

						<!-- Content Area -->
						<div style="padding: 2rem; display: flex; align-items: center; gap: 2rem; min-height: 220px;">
							
							<!-- Game Image -->
							<div style="flex-shrink: 0;">
								<div style="width: 10rem; height: 10rem; border-radius: 16px; overflow: hidden; box-shadow: 0 8px 20px rgba(0, 172, 193, 0.3); position: relative; background: linear-gradient(135deg, rgba(0, 172, 193, 0.1), rgba(255, 183, 77, 0.1));">
									<!-- Fixed placeholder to prevent CLS -->
									<div style="width: 160px; height: 160px; background: linear-gradient(135deg, rgba(0, 172, 193, 0.1), rgba(255, 183, 77, 0.1)); position: absolute; top: 0; left: 0; border-radius: 16px;"></div>
									<img src="/images/zonenova.jpg" alt="Zone Nova" style="width: 160px; height: 160px; object-fit: cover; position: relative; z-index: 1; border-radius: 16px;" loading="eager" decoding="async" width="160" height="160" fetchpriority="high">
								</div>
							</div>

							<!-- Game Info -->
							<div style="flex: 1; min-height: 180px;">
								<h3 style="font-family: 'Space Grotesk', sans-serif; font-size: 2.5rem; font-weight: 700; color: var(--text-primary); margin: 0 0 1rem 0; line-height: 1.2;">
									Zone Nova
								</h3>
								<p style="color: var(--text-secondary); font-size: 1.1rem; line-height: 1.6; margin: 0 0 1rem 0; max-width: 35rem;">
									Character guides, runes, events, and announcements.
								</p>

								<!-- Active Events Section -->
								<div style="background: linear-gradient(135deg, rgba(0, 172, 193, 0.1), rgba(255, 183, 77, 0.1)); border-radius: 12px; padding: 1rem; margin: 0 0 1rem 0; border-left: 4px solid var(--teal-crystal);">
									<div style="font-size: 0.9rem; font-weight: 600; color: var(--amber-glow); margin-bottom: 0.75rem; display: flex; align-items: center; gap: 0.5rem;">
										<span>🌀</span> Active Events
									</div>
									<div style="display: flex; flex-direction: column; gap: 0.5rem;">
										<a href="/guides/zone-nova/rifts/rift-tide-4/" style="text-decoration: none; color: inherit;">
											<div style="background: rgba(0, 172, 193, 0.15); padding: 0.75rem; border-radius: 6px; min-height: 3.5rem; display: flex; align-items: center; justify-content: space-between; cursor: pointer; transition: all 0.3s ease; border: 1px solid rgba(0, 172, 193, 0.2);" onmouseover="this.style.transform='translateY(-2px)'; this.style.background='rgba(0, 172, 193, 0.25)'; this.style.borderColor='rgba(0, 172, 193, 0.4)'" onmouseout="this.style.transform='translateY(0)'; this.style.background='rgba(0, 172, 193, 0.15)'; this.style.borderColor='rgba(0, 172, 193, 0.2)'">
												<div style="font-weight: 600; color: var(--text-primary); font-size: 0.85rem;">Rift Tide IV</div>
												<div id="riftTide4Timer" style="color: var(--teal-crystal); font-weight: 600; font-family: 'Courier New', monospace; font-size: 0.75rem;">Loading...</div>
											</div>
										</a>
										<a href="/guides/zone-nova/rifts/rift-tide-3/" style="text-decoration: none; color: inherit;">
											<div style="background: rgba(255, 183, 77, 0.15); padding: 0.75rem; border-radius: 6px; min-height: 3.5rem; display: flex; align-items: center; justify-content: space-between; cursor: pointer; transition: all 0.3s ease; border: 1px solid rgba(255, 183, 77, 0.2);" onmouseover="this.style.transform='translateY(-2px)'; this.style.background='rgba(255, 183, 77, 0.25)'; this.style.borderColor='rgba(255, 183, 77, 0.4)'" onmouseout="this.style.transform='translateY(0)'; this.style.background='rgba(255, 183, 77, 0.15)'; this.style.borderColor='rgba(255, 183, 77, 0.2)'">
												<div style="font-weight: 600; color: var(--text-primary); font-size: 0.85rem;">Rift Tide III</div>
												<div id="riftTide3Timer" style="color: var(--amber-glow); font-weight: 600; font-family: 'Courier New', monospace; font-size: 0.75rem;">Loading...</div>
											</div>
										</a>
										<a href="/guides/zone-nova/characters/yuis/" style="text-decoration: none; color: inherit;">
											<div style="background: rgba(255, 77, 77, 0.15); padding: 0.75rem; border-radius: 6px; min-height: 3.5rem; display: flex; align-items: center; justify-content: space-between; cursor: pointer; transition: all 0.3s ease; border: 1px solid rgba(255, 77, 77, 0.2);" onmouseover="this.style.transform='translateY(-2px)'; this.style.background='rgba(255, 77, 77, 0.25)'; this.style.borderColor='rgba(255, 77, 77, 0.4)'" onmouseout="this.style.transform='translateY(0)'; this.style.background='rgba(255, 77, 77, 0.15)'; this.style.borderColor='rgba(255, 77, 77, 0.2)'">
												<div style="font-weight: 600; color: var(--text-primary); font-size: 0.85rem;">New Characters</div>
												<div style="color: var(--rose-quartz); font-weight: 600; font-size: 0.75rem;">Yuis Available</div>
											</div>
										</a>
									</div>
								</div>

								<p style="color: var(--amber-glow); font-size: 0.9rem; font-weight: 500; margin: 0 0 2rem 0;">
									Last updated: {new Date().toLocaleDateString('en-US', { month: 'short', day: 'numeric', year: 'numeric' })}
								</p>
								
								<!-- Action Button -->
								<div style="background: var(--amber-glow); color: white; padding: 1rem 2rem; border-radius: 25px; font-family: 'Space Grotesk', sans-serif; font-size: 1rem; font-weight: 600; display: inline-flex; align-items: center; gap: 0.75rem; will-change: transform, background-color; transform: translateZ(0); cursor: pointer; min-height: 48px;" class="action-button">
									Explore Zone Nova
									<span style="font-size: 1.25rem;" aria-hidden="true">→</span>
								</div>
							</div>
						</div>
					</a>
				</div>
			</div>

			<!-- Other Games Section -->
			<div style="text-align: center; margin-bottom: 2rem;">
				<h3 style="font-family: 'Space Grotesk', sans-serif; font-size: 1.2rem; font-weight: 600; background: linear-gradient(135deg, var(--amber-glow), var(--teal-crystal)); -webkit-background-clip: text; -webkit-text-fill-color: transparent; margin: 0; contain: layout style paint;">
					More Games Coming Soon
				</h3>
			</div>

			<!-- Coming Soon Grid -->
			<div style="display: grid; grid-template-columns: repeat(auto-fit, minmax(250px, 1fr)); gap: 1.5rem;">
				
				<!-- Coming Soon Card 1 -->
				<div style="background: var(--bg-primary); border-radius: 16px; padding: 2rem; border: 1px solid var(--mocha-mousse); text-align: center; opacity: 0.8; will-change: transform, opacity; transform: translateZ(0); min-height: 180px; contain: layout style;" class="coming-soon-card">
					<div style="width: 4rem; height: 4rem; border-radius: 12px; background: linear-gradient(135deg, var(--mocha-mousse), var(--amber-glow)); display: flex; align-items: center; justify-content: center; margin: 0 auto 1.5rem auto;">
						<span style="font-size: 2rem;" aria-hidden="true">🎮</span>
					</div>
					<h4 style="font-family: 'Space Grotesk', sans-serif; font-size: 1.3rem; font-weight: 600; color: var(--text-primary); margin: 0 0 0.75rem 0;">Coming Soon</h4>
					<div style="background: var(--mocha-mousse); color: white; padding: 0.4rem 1rem; border-radius: 20px; font-size: 0.8rem; font-weight: 600; display: inline-block;">
						In Development
					</div>
				</div>

				<!-- Coming Soon Card 2 -->
				<div style="background: var(--bg-primary); border-radius: 16px; padding: 2rem; border: 1px solid var(--rose-quartz); text-align: center; opacity: 0.8; will-change: transform, opacity; transform: translateZ(0); min-height: 180px; contain: layout style;" class="coming-soon-card">
					<div style="width: 4rem; height: 4rem; border-radius: 12px; background: linear-gradient(135deg, var(--rose-quartz), var(--teal-crystal)); display: flex; align-items: center; justify-content: center; margin: 0 auto 1.5rem auto;">
						<span style="font-size: 2rem;" aria-hidden="true">⏳</span>
					</div>
					<h4 style="font-family: 'Space Grotesk', sans-serif; font-size: 1.3rem; font-weight: 600; color: var(--text-primary); margin: 0 0 0.75rem 0;">Coming Soon</h4>
					<div style="background: var(--rose-quartz); color: white; padding: 0.4rem 1rem; border-radius: 20px; font-size: 0.8rem; font-weight: 600; display: inline-block;">
						Stay Tuned
					</div>
				</div>

				<!-- Coming Soon Card 3 -->
				<div style="background: var(--bg-primary); border-radius: 16px; padding: 2rem; border: 1px solid var(--teal-crystal); text-align: center; opacity: 0.8; will-change: transform, opacity; transform: translateZ(0); min-height: 180px; contain: layout style;" class="coming-soon-card">
					<div style="width: 4rem; height: 4rem; border-radius: 12px; background: linear-gradient(135deg, var(--teal-crystal), var(--amber-glow)); display: flex; align-items: center; justify-content: center; margin: 0 auto 1.5rem auto;">
						<span style="font-size: 2rem;" aria-hidden="true">🔮</span>
					</div>
					<h4 style="font-family: 'Space Grotesk', sans-serif; font-size: 1.3rem; font-weight: 600; color: var(--text-primary); margin: 0 0 0.75rem 0;">Coming Soon</h4>
					<div style="background: var(--teal-crystal); color: white; padding: 0.4rem 1rem; border-radius: 20px; font-size: 0.8rem; font-weight: 600; display: inline-block;">
						Future Release
					</div>
				</div>
			</div>
		</section>

		<!-- "Built by" Section -->
		<section style="text-align: center; margin-bottom: 6rem; contain: layout style;">
			<div style="opacity: 0; transform: translateY(30px);" class="fade-in-section">
				<p style="font-family: 'Space Grotesk', sans-serif; font-size: clamp(1.5rem, 3vw, 2rem); font-weight: 600; line-height: 1.4; max-width: 42rem; margin: 0 auto; background: linear-gradient(135deg, var(--amber-glow), var(--teal-crystal)); -webkit-background-clip: text; -webkit-text-fill-color: transparent; text-shadow: 0 0 30px rgba(255, 183, 77, 0.3);">
					Built by someone who's spent way too much time on gacha games...
				</p>
			</div>
		</section>

		<!-- Floating Games Modal -->
		<div id="gamesModal" style="position: fixed; top: 0; left: 0; width: 100%; height: 100%; background: rgba(0,0,0,0.8); backdrop-filter: blur(10px); z-index: 1000; opacity: 0; visibility: hidden; transition: opacity 0.3s ease, visibility 0.3s ease;">
			<div style="display: flex; align-items: center; justify-content: center; height: 100%; padding: 2rem;">
				<div style="background: var(--bg-primary); border-radius: 20px; padding: 2rem; max-width: 60rem; width: 100%; max-height: 80vh; overflow-y: auto; border: 1px solid var(--amber-glow); box-shadow: 0 20px 50px rgba(0,0,0,0.5); transform: scale(0.9); transition: transform 0.3s ease;" id="modalContent">
					
					<!-- Modal Header -->
					<div style="display: flex; justify-content: space-between; align-items: center; margin-bottom: 2rem;">
						<h2 style="font-family: 'Space Grotesk', sans-serif; font-size: 2rem; font-weight: 700; color: var(--text-primary); margin: 0; background: linear-gradient(135deg, var(--amber-glow), var(--teal-crystal)); -webkit-background-clip: text; -webkit-text-fill-color: transparent;">
							Choose Your Game
						</h2>
						<button id="closeModal" style="background: none; border: none; color: var(--text-secondary); font-size: 2rem; cursor: pointer; padding: 0.5rem; transition: color 0.2s ease;" aria-label="Close modal">
							×
						</button>
					</div>

					<!-- Games Grid -->
					<div style="display: grid; grid-template-columns: repeat(auto-fit, minmax(280px, 1fr)); gap: 1.5rem;">
						
						<!-- Zone Nova Card -->
						<div style="background: var(--bg-secondary); border-radius: 16px; padding: 1.5rem; border: 1px solid var(--amber-glow); will-change: transform; transform: translateZ(0);" class="modal-card">
							<a href="/guides/zone-nova/" style="text-decoration: none; color: inherit;">
								<div style="width: 100%; height: 150px; border-radius: 12px; overflow: hidden; margin-bottom: 1rem; box-shadow: 0 0 20px rgba(0, 172, 193, 0.3); position: relative; background: linear-gradient(135deg, rgba(0, 172, 193, 0.1), rgba(255, 183, 77, 0.1));">
									<!-- Fixed placeholder -->
									<div style="width: 280px; height: 150px; background: linear-gradient(135deg, rgba(0, 172, 193, 0.1), rgba(255, 183, 77, 0.1)); position: absolute; top: 0; left: 0; border-radius: 12px;"></div>
									<img src="/images/zonenova.jpg" alt="Zone NOVA" style="width: 280px; height: 150px; object-fit: cover; position: relative; z-index: 1; border-radius: 12px;" loading="lazy" decoding="async" width="280" height="150">
								</div>
								<h3 style="font-family: 'Space Grotesk', sans-serif; font-size: 1.5rem; font-weight: 700; color: var(--text-primary); margin: 0 0 0.5rem 0;">Zone Nova</h3>
								<p style="color: var(--text-secondary); font-size: 0.9rem; margin-bottom: 1rem;">Character guides, runes, events, and announcements</p>
								<div style="display: flex; gap: 0.5rem;">
									<span style="background: var(--amber-glow); color: white; padding: 4px 8px; border-radius: 8px; font-size: 0.7rem;">4 Sections</span>
									<span style="background: var(--teal-crystal); color: white; padding: 4px 8px; border-radius: 8px; font-size: 0.7rem;">Active</span>
								</div>
							</a>
						</div>

						<!-- Coming Soon Cards -->
						<div style="background: var(--bg-secondary); border-radius: 16px; padding: 1.5rem; border: 1px solid var(--mocha-mousse); opacity: 0.6; transform: translateZ(0); min-height: 250px;">
							<div style="width: 100%; height: 150px; border-radius: 12px; background: linear-gradient(135deg, var(--mocha-mousse), var(--amber-glow)); display: flex; align-items: center; justify-content: center; margin-bottom: 1rem;">
								<span style="font-size: 3rem;" aria-hidden="true">🚧</span>
							</div>
							<h3 style="font-family: 'Space Grotesk', sans-serif; font-size: 1.5rem; font-weight: 700; color: var(--text-primary); margin: 0 0 0.5rem 0;">Coming Soon</h3>
							<p style="color: var(--text-secondary); font-size: 0.9rem; margin-bottom: 1rem;">More games in development</p>
							<span style="background: var(--mocha-mousse); color: white; padding: 4px 8px; border-radius: 8px; font-size: 0.7rem;">Soon™</span>
						</div>

					</div>
				</div>
			</div>
		</div>

	</main>
</HomeLayout>

<style>
/* Performance-optimized animations */
@keyframes fadeInScale {
	0% {
		opacity: 0;
		transform: scale(0.95) translateY(20px);
	}
	100% {
		opacity: 1;
		transform: scale(1) translateY(0);
	}
}

@keyframes titleGlow {
	0% {
		text-shadow: 0 0 10px rgba(255, 183, 77, 0.3);
		filter: brightness(1);
	}
	100% {
		text-shadow: 0 0 20px rgba(255, 183, 77, 0.6);
		filter: brightness(1.1);
	}
}

/* Optimized hover effects with GPU acceleration */
.title-hover {
	transition: transform 0.2s ease;
}

.title-hover:hover {
	transform: scale(1.02) translateZ(0);
}

.featured-card {
	transition: transform 0.3s ease, box-shadow 0.3s ease;
}

.featured-card:hover {
	transform: translateY(-2px) translateZ(0);
	box-shadow: 0 15px 35px rgba(164, 120, 100, 0.3);
}

.action-button {
	transition: transform 0.2s ease, background-color 0.2s ease;
}

.action-button:hover {
	background-color: #e6a843 !important;
	transform: translateX(2px) translateZ(0);
}

.coming-soon-card {
	transition: transform 0.3s ease, opacity 0.3s ease;
}

.coming-soon-card:hover {
	opacity: 1 !important;
	transform: translateY(-2px) translateZ(0);
}

.modal-card {
	transition: transform 0.3s ease, border-color 0.3s ease;
}

.modal-card:hover {
	transform: translateY(-4px) translateZ(0);
	border-color: var(--teal-crystal) !important;
}

/* Fade-in animation for sections */
.fade-in-section {
	transition: opacity 1s ease, transform 1s ease;
}

.fade-in-section.visible {
	opacity: 1 !important;
	transform: translateY(0) !important;
}

/* Mobile Responsive Design - Optimized */
@media (max-width: 768px) {
	main {
		padding: 6rem 1rem 2rem 1rem !important;
		max-width: 100% !important;
	}

	/* Reduce animations on mobile for better performance */
	.title-hover,
	.featured-card,
	.action-button,
	.coming-soon-card,
	.modal-card {
		transition: none;
		will-change: auto;
	}

	/* Touch-optimized hover states */
	@media (hover: none) {
		.title-hover:hover,
		.featured-card:hover,
		.action-button:hover,
		.coming-soon-card:hover,
		.modal-card:hover {
			transform: none;
			background-color: initial;
			opacity: initial;
			box-shadow: initial;
			border-color: initial;
		}
	}

	/* Game Guide Library Title */
	h2 {
		font-size: clamp(2rem, 6vw, 2.5rem) !important;
		margin-bottom: 1rem !important;
	}

	/* Zone Nova Card Mobile Layout */
	div[style*="padding: 2rem; display: flex; align-items: center; gap: 2rem;"] {
		flex-direction: column !important;
		text-align: center !important;
		gap: 1.5rem !important;
		padding: 1.5rem !important;
	}

	/* Game Image Mobile */
	div[style*="width: 10rem; height: 10rem;"] {
		width: 8rem !important;
		height: 8rem !important;
		margin: 0 auto !important;
	}

	/* Game Title Mobile */
	h3[style*="font-size: 2.5rem;"] {
		font-size: 2rem !important;
		margin-bottom: 1rem !important;
	}

	/* Active Events Section Mobile */
	div[style*="display: flex; flex-direction: column;"] {
		gap: 0.4rem !important;
	}

	/* Event Cards Mobile */
	div[style*="min-height: 3.5rem;"] {
		min-height: 3rem !important;
		padding: 0.6rem !important;
		font-size: 0.8rem !important;
	}

	/* Action Button Mobile */
	.action-button {
		padding: 0.875rem 1.5rem !important;
		font-size: 0.9rem !important;
		margin-top: 1rem !important;
		width: fit-content !important;
		margin-left: auto !important;
		margin-right: auto !important;
	}

	/* Coming Soon Cards Mobile */
	.coming-soon-card {
		padding: 1.5rem !important;
	}

	/* Modal Mobile */
	#gamesModal div[style*="padding: 2rem;"] {
		padding: 1rem !important;
		margin: 1rem !important;
		max-height: 90vh !important;
	}

	/* Modal Games Grid Mobile */
	#gamesModal div[style*="grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));"] {
		grid-template-columns: 1fr !important;
		gap: 1rem !important;
	}
}

/* Extra Small Mobile (iPhone SE, etc.) */
@media (max-width: 480px) {
	main {
		padding: 5rem 0.75rem 1.5rem 0.75rem !important;
	}

	h2 {
		font-size: 1.8rem !important;
	}

	div[style*="width: 10rem; height: 10rem;"] {
		width: 6rem !important;
		height: 6rem !important;
	}

	h3[style*="font-size: 2.5rem;"] {
		font-size: 1.6rem !important;
	}

	.action-button {
		padding: 0.75rem 1.25rem !important;
		font-size: 0.85rem !important;
	}

	.coming-soon-card {
		padding: 1.25rem !important;
	}
}

/* Reduce motion for accessibility */
@media (prefers-reduced-motion: reduce) {
	*,
	*::before,
	*::after {
		animation-duration: 0.01ms !important;
		animation-iteration-count: 1 !important;
		transition-duration: 0.01ms !important;
	}
}
</style>

<script>
// Optimized modal functionality with passive event listeners
document.addEventListener('DOMContentLoaded', function() {
	const modal = document.getElementById('gamesModal');
	const modalContent = document.getElementById('modalContent');
	const closeBtn = document.getElementById('closeModal');
	
	// Intersection Observer for fade-in animations
	const observerOptions = {
		threshold: 0.1,
		rootMargin: '0px 0px -50px 0px'
	};
	
	const observer = new IntersectionObserver((entries) => {
		entries.forEach(entry => {
			if (entry.isIntersecting) {
				entry.target.classList.add('visible');
				observer.unobserve(entry.target);
			}
		});
	}, observerOptions);
	
	// Observe fade-in sections
	const fadeInSections = document.querySelectorAll('.fade-in-section');
	fadeInSections.forEach(section => observer.observe(section));
	
	// Modal functionality
	closeBtn?.addEventListener('click', () => {
		if (modal && modalContent) {
			modal.style.opacity = '0';
			modal.style.visibility = 'hidden';
			modalContent.style.transform = 'scale(0.9)';
		}
	}, { passive: true });
	
	modal?.addEventListener('click', (e) => {
		if (e.target === modal && modalContent) {
			modal.style.opacity = '0';
			modal.style.visibility = 'hidden';
			modalContent.style.transform = 'scale(0.9)';
		}
	}, { passive: true });
});

// Optimized Event Timers with requestAnimationFrame
document.addEventListener('DOMContentLoaded', function() {
	// Set end dates for events (updated to match rifts page)
	const rift3End = new Date('2025-06-23T23:59:59').getTime(); // Rift Tide III - ends June 23, 2025
	const rift4End = new Date('2025-07-08T23:59:59').getTime(); // Rift Tide IV - ends July 8, 2025

	let timerRAF;

	// Timer function optimized for performance
	function updateTimers() {
		const now = new Date().getTime();
		
		// Update Rift Tide III timer
		const rift3Timer = document.getElementById('riftTide3Timer');
		if (rift3Timer) {
			const timeLeft = rift3End - now;
			if (timeLeft > 0) {
				const days = Math.floor(timeLeft / (1000 * 60 * 60 * 24));
				const hours = Math.floor((timeLeft % (1000 * 60 * 60 * 24)) / (1000 * 60 * 60));
				rift3Timer.textContent = `${days}d ${hours}h`;
			} else {
				rift3Timer.textContent = 'Ended';
			}
		}

		// Update Rift Tide IV timer  
		const rift4Timer = document.getElementById('riftTide4Timer');
		if (rift4Timer) {
			const timeLeft = rift4End - now;
			if (timeLeft > 0) {
				const days = Math.floor(timeLeft / (1000 * 60 * 60 * 24));
				const hours = Math.floor((timeLeft % (1000 * 60 * 60 * 24)) / (1000 * 60 * 60));
				rift4Timer.textContent = `${days}d ${hours}h`;
			} else {
				rift4Timer.textContent = 'Ended';
			}
		}
	}

	// Optimized timer updates (every 30 seconds instead of 1 minute)
	updateTimers();
	setInterval(updateTimers, 30000);
});

// Preload critical resources
const preloadLink = document.createElement('link');
preloadLink.rel = 'preload';
preloadLink.href = '/images/zonenova.jpg';
preloadLink.as = 'image';
document.head.appendChild(preloadLink);
</script>
